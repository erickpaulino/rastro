# Copy this file to ".env" and replace the placeholder values with those of your environment.

# Database connection
DB_HOST=localhost
DB_NAME=rastro
DB_USER=rastro
DB_PASS=change-me

# Authentication
# Provide a JSON object where the keys are usernames and the values are
# bcrypt hashes generated via PHP's password_hash helper.
# Example: php -r "echo password_hash('strong-password', PASSWORD_DEFAULT), PHP_EOL;"
# Never commit real hashes to the repository.
RASTRO_USERS_JSON={"erick":"$2y$10$REPLACE_WITH_BCRYPT_HASH"}
